<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Para emulador -->
    <domain-config cleartextTrafficPermitted="true">
        <domain>10.0.2.2</domain>
    </domain-config>

    <!-- Para teléfono real -->
    <domain-config cleartextTrafficPermitted="true">
        <domain>192.168.1.65</domain> <!-- Cambia esto por la IP real de tu PC si es distinta -->
    </domain-config>
</network-security-config>
